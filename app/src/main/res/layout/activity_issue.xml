<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/swipeRefreshIssue"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                tools:context=".activities.IssueActivity">
                <include layout="@layout/toolbar_top"/>
                <android.support.v7.widget.SwitchCompat
                        android:paddingLeft="10dp"
                        android:id="@+id/switchCompatIssues"
                        android:layout_width="wrap_content"
                        android:layout_height="50dp"
                        android:checked="false"
                        android:text="@string/myIssues"/>
                <ListView
                        android:id="@+id/listView"
                        android:layout_weight="1"
                        android:layout_width="match_parent"
                        android:layout_height="0dip"/>
                <TextView
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:gravity="center"
                        android:id="@+id/issuesPlaceholder"
                        android:text="@string/onLoadPlaceholder"/>
                <Button
                        android:id="@+id/loadMoreButton"
                        android:background="@drawable/button"
                        android:text="@string/loadMoreButton"
                        android:layout_width="match_parent"
                        android:textColor="#fff"
                        android:layout_height="wrap_content"
                        android:onClick="loadMore"/>
        </LinearLayout>
</android.support.v4.widget.SwipeRefreshLayout>